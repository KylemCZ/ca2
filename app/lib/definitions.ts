export type Technology = {
  id: string;
  description: string;
  image: string;
  bgColor: string;
  buttonColor: string;
  sort_order: number;
};

export type Projects = {
  id: string;
  description: string;
  image: string;
  bgColor: string;
  buttonColor: string;
  sort_order: number;
};
